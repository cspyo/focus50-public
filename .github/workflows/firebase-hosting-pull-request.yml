# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up npm
        run: |
          npm -v
          npm install -g npm
      - name: List working directory
        run: |
          cd ${{ github.workspace }}
          sudo apt install -y tree
          tree ${{ github.workspace }}
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.0.2'
          channel: 'stable'
      - run: flutter --version
      - name: Setting .env
        run: |
          mkdir ${{ github.workspace }}/assets/config
          echo "FIREBASE_API_KEY=${{ secrets.FIREBASE_API_KEY }}" >> ${{ github.workspace }}/assets/config/.env
          echo "FIREBASE_APP_ID=${{ secrets.FIREBASE_APP_ID }}" >> ${{ github.workspace }}/assets/config/.env
          echo "FIREBASE_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}" >> ${{ github.workspace }}/assets/config/.env
          echo "FIREBASE_PROJECT_ID=${{ secrets.FIREBASE_PROJECT_ID }}" >> ${{ github.workspace }}/assets/config/.env
          echo "FIREBASE_AUTH_DOMAIN=${{ secrets.FIREBASE_AUTH_DOMAIN }}" >> ${{ github.workspace }}/assets/config/.env
          echo "FIREBASE_STORAGE_BUCKET=${{ secrets.FIREBASE_STORAGE_BUCKET }}" >> ${{ github.workspace }}/assets/config/.env
      - run: flutter pub get && flutter build web --release
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FOCUS50_8B405 }}'
          projectId: focus50-8b405
          expires: 30d
