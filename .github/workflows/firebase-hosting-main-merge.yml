# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up npm
        run: |
          npm -v
          npm install -g npm
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.0.2'
          channel: 'stable'
      - run: flutter --version
      - name: Setting firebase_options.dart
        run: |
          chmod u+x ${{ github.workspace }}/.github/create_firebase_options.dart.sh
          sh ${{ github.workspace }}/.github/create_firebase_options.dart.sh ${{ secrets.FIREBASE_API_KEY }} ${{ secrets.FIREBASE_APP_ID }} ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }} ${{ secrets.FIREBASE_PROJECT_ID }} ${{ secrets.FIREBASE_AUTH_DOMAIN }} ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          ls ${{ github.workspace }}/lib
      - run: flutter pub get && flutter build web --release
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FOCUS50_8B405 }}'
          channelId: live
          projectId: focus50-8b405
